#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@gov.au/pancake/bin/pancake" "$@"
  ret=$?
else 
  node  "$basedir/../@gov.au/pancake/bin/pancake" "$@"
  ret=$?
fi
exit $ret
